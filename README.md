# DOCX Placeholder Filler (Next.js)

Next.js + TypeScript + Tailwind + Docker app that:

- Uploads a .docx template
- Detects {{PLACEHOLDER}} tags
- Dynamically generates input fields
- Produces a filled .docx document

## Run locally

docker compose up --build